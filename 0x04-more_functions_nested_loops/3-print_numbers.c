#include "main.h"
#include <stdio.h>

/**
 * print_numbers - prints numbers from 0 to 9 followed by a new line
 *
 * Return: (0)
 */
void print_numbers(void)
{
int i = 0;

for (i = 0; i <= 9; i++)
{
printf("%d\n", i);
}

return (0);
}
