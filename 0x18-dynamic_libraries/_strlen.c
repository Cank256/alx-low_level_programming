#include "main.h"
#include <stdio.h>

/**
 * _strlen - Computes the length of a string
 * @s: The string to compute the length of
 *
 * Return: The length of @s.
 */
int _strlen(char *s)
{
return (0);
}
